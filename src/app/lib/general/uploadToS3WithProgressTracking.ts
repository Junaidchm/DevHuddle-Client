export const uploadToS3 = async (
  file: File,
  presignedUrl: string,
  onProgress: (progress: number) => void,
  signal?: AbortSignal
) => {

    try {
             
    } catch (err:any) {
        
    }

};
